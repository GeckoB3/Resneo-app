// Shared TypeScript types — expand as features are built (Phase 2+).

/** Placeholder until domain types are ported from reserve-ni. */
export type BuildPhase = 'foundation' | 'auth' | 'today' | 'release';
